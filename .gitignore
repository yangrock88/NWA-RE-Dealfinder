# Virtual environment
.venv/
__pycache__/
*.pyc
*.pyo

# Generated output — these are rebuilt on every run
deals_report.html

# Runtime data — snapshot and logs update every 6 hours; no need to track history
data/latest.json
data/scheduler.log
data/task.xml

# Keep the data directory itself (has .gitkeep)
!data/.gitkeep

# uv cache
.uv/

# Windows artifacts
Thumbs.db
desktop.ini
